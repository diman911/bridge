{
  "name": "@fairlead/bridge-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker adapter for the cloud-mode Fairlead Bridge",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "dev": "wrangler dev --local"
  },
  "dependencies": {
    "@fairlead/bridge-core": "0.1.0"
  }
}
