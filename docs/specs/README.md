# Specs index

Update this table (file → what it covers → which package) as specs are added.

| File                                                 | Covers                                                                                                       | Package       |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------- |
| [`bridge-core-contract.md`](bridge-core-contract.md) | Envelope/`Connector` contract: protocol version, write path and update merge, read path, conformance testing | `bridge-core` |
| [`bridge-worker.md`](bridge-worker.md)               | Cloud Worker authentication, trusted Control Plane resolution, connector dispatch, and request deadlines   | `bridge-worker` |
