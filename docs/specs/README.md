# Specs index

Update this table (file → what it covers → which package) as specs are added.

| File                                                    | Covers                                                                                                                     | Package       |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------- |
| [`bridge-core-contract.md`](bridge-core-contract.md)    | `IntegrationCommand`/`Connector` wire contract: protocol version, write path, read path, validation, conformance testing     | `bridge-core` |
